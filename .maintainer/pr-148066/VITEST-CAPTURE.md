# PR148066 prospective first-use capture

This proof-only revision replaces the fixed workflow's previous standalone-Node omission matrix. One fresh hosted run, one Windows job, one unchanged missing-task baseline and one separately labelled instrumented cell. A pass is NON_REPRODUCTION and stops; there is no automatic retry or product patch. This is not historical reconstruction or native acceptance.

The workflow pins historical source7c532c395d62d36b989a0f35bf15ccafb90c6668, Node24.20.0 and actions. All20 source/config/package/lock bindings are checked before deriving the read-only fixture. Same pinned setup-pnpm action and frozen dependencies; no cache restore/clear, prewarm, Defender change, installer, registration, activation, new product tuple, live Odin or119052 custody.

The original router selects the original unit-fast threads/isolate:false config and setup.env.ts/hermetic initializer. Worker/serial variables match recorded settings. The same runtime-build command runs immediately before the router; unlike the original full file list, this selected fixture does not itself request a build. Only this original file is selected. Original registration/found/delete are physically absent and OUT OF SCOPE, never passed or waived. The original task prefix+UUID, call and assertion are preserved exactly. No fixture timeout or child timeout is widened.

A restorable builtin-export observer forwards the production native spawnSync call; no production module is rewritten. Baseline executable, args object, options object, manager projector,5000ms,windowsHide:false and original script are unchanged. Metadata-only snapshots and writes introduce measured overhead. Output copies are bounded64KiB without changing native spawn options or return values; native default output limit remains unchanged. The instrumented cell runs in afterAll even on assertion failure, after the baseline, sharing its worker/home/cache. It adds stderr entry and phase markers but retains options/env/budget. Missing markers cannot distinguish COM activation from command discovery. Binary-content hashes and PowerShell version/account/session queries are explicitly afterward. No seven-key omission.

Known differences: fresh image/binaries/cache/home/UUID, no historical Defender exclusions, selected read-only fixture instead of full Windows file list, explicit runtime-build call rather than full-suite preflight selection, added builtin-forwarder/observation overhead and routing preflight. Prior runner activity is not claimed cold or reconstructed. Environment output is allowlisted routing/worker values only; never whole process environment or credentials.

Driver records finite child PIDs/logs, bounded elapsed execution, outcome and postflight live-process inventory before removing exact task-private source/dependencies/build and hermetic namespace. Cleanup also runs after setup failure; if live writers cannot be excluded, it retains paths with a failure receipt. Hosted teardown is not counted as observed cleanup.

Retain product66a495 unchanged and all draft/substantive-review/L8/c064/F1,735 combined tuple, installed updater x real Gateway throttle/progress/stale/owner-death, Apple/native/resulting-head CI/LAND holds. Samefd7a/5b9a owns this phase and PR.

## Consumed run and undispatched correction

Run35551172573 at1b18976d passed a baseline in2729.9203ms but executed daemon instead of required unit-fast. Original content classifier rejects module-mocking, bare test-mocking API and dynamic imports; the initial fixture changed classification. This run is NOT a qualified requested-envelope NON_REPRODUCTION or historical repair. The one-run allowance is consumed; no further run is authorized by this revision.

Correction removes test-module mocking, uses a restored builtin-export forwarder, and requires original router preflight to select unit-fast before the build/test. The worker requires a threads identity plus original setup.env.ts symbol; final outcome requires exactly one baseline/one instrumented cell and actual unit-fast startup log. No forced router/config membership or product source edit. Corrected code requires independent delta review and a newly authorized run before native qualification.
